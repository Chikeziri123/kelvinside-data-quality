name: 'kelvinside_dq'
version: '1.0.0'
config-version: 2

profile: 'kelvinside_dq'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  kelvinside_dq:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts